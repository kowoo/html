EL표현식(Expression Language) (표현 언어)
${값}
JSP 페이지 내에서 자바코드의 값을 쉽게 출력하기 위해서 사용
<%= %> 표현부와 비슷한 역할
왜 언어?
${값(연산이 가능)}
표현 언어 사용법에 대해서 공부

JSTL (JSP Standard Tag Library)
	JSP에서 자주 사용되는 유용한 태그를 모아놓은 라이브러리
	core, format
	적용 방법
		mvn repository > jstl 검색 > 1.2 > jar 다운로드 > 받은 파일을 프로젝트 > 웹콘텐트 > lib에 복사