html 요소는 두 가지로 분류
 1. block요소: 화면의 가로 영역을 모두 차지하는 요소, 다른 요소가 침범을 못함. (그냥 줄넘김 기능이 같이 있다고 봐도 될듯?)
 2. inline요소: 내용만큼만 영역을 차지하기 때문에 옆으로 다른 인라인 요소가 올 수 있다.
 
html 태그 종류
 <h>: 문서내에서 제목을 표시할 때 사용, 1~6까지 중요도 순으로 사용
 <p>: 문단을 만드는 역할
 <b>, <strong>: 볼드
 <mark>: 강조(형광펜)
 <blockquote>: 인용문
 <hr>: 개행+줄긋기
 <i>, <em>: 이탤릭
 *<span>: 요소 내에서 특정 영역을 지정할 때 사용
 
 <form>: 사용자로부터 값을 입력받고 서버로 전송하기 위한 태그
 하위태그로 <input> 태그를 가지면서 정보를 입력받을 수 있다.
 <input type="종류">
 type 종류
  text: 문자열
  password: 문자열, 가려져서 화면 출력
  email: text와 동일하나 입력패턴을 검사해서 email주소인지 확인함
  submit: 버튼, form 하위 요소의 내용을 서버로 전송하는 버튼, form요소의 action에 선언된 요청이 실행
  button: 버튼모양의 요소가 출력
  reset: 버튼, form 하위 input요소의 내용을 모두 비움
  radio: 항목출력, 단일 선택 가능한 체크박스
  checkbox: 항목출력, 다중 선택 가능한 체크박스
  file: 파일업로드를 위해서 파일 선택가능한 요소
  number: 숫자입력(최대, 최소 등 지정 가능)
  date: 날짜입력 요소


